name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Build the scout-backend Docker image
      run: |
        echo $ACTIONS | docker login ghcr.io -u m-valkov --password-stdin
        docker build --target=stage . --file Dockerfile --tag ghcr.io/m-valkov/scout-backend:stage
        docker push ghcr.io/m-valkov/scout-backend:stage
        docker build --target=production . --file Dockerfile --tag ghcr.io/m-valkov/scout-backend:productions
        docker push ghcr.io/m-valkov/scout-backend:productions
      env:
        ACTIONS: ${{ secrets.ACTIONS }}
